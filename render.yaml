databases:
  - name: comex-db
    plan: starter
    databaseName: comex_db
    user: professor_iesb

services:
  - type: web
    name: comex-dashboard
    runtime: docker
    plan: starter
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: comex-db
          property: connectionString
