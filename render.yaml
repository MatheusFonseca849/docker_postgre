databases:
  - name: comex-db
    plan: free
    databaseName: comex_db
    user: professor_iesb

services:
  - type: web
    name: comex-dashboard
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: comex-db
          property: connectionString
